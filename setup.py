import setuptools
setuptools.setup(name='vfe',
      version='0.1',
      description='',
      author='',
      author_email='',
      url='',
      packages=setuptools.find_packages(exclude=('datasets',))
     )
